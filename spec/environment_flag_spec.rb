require 'spec_helper'

describe EnvironmentFlag do
  it 'has a version number' do
    expect(EnvironmentFlag::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
